# Optional base Nginx snippet for YFCDN edge nodes.
# The agent writes yfcdn-managed.conf with proxy_cache_path and server blocks.
# Make sure /var/cache/yfcdn exists and is writable by the nginx worker user.

worker_rlimit_nofile 65535;
